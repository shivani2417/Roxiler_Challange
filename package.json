{
  "name": "mern_stack_project-(1)",
  "version": "1.0.0",
  "description": "## Backend 1. Navigate to the backend directory:    ```bash    cd backend    ``` 2. Install dependencies:    ```bash    npm install    ``` 3. Start the server:    ```bash    npm run dev    ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "express": "^4.21.2",
    "mongoose": "^8.9.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
